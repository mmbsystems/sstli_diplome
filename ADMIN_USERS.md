# إدارة حسابات موظفي SSTLI

التطبيق خاص بالموظفين. يحتوي `config/admins.json` على حسابات الموظفين بتجزئات bcrypt فقط؛ لا توجد كلمات مرور افتراضية في المصدر. لا تستبدل الملف بمصفوفة فارغة عند إعداد النسخة الحالية.

## الإعداد الأول

1. شغّل `npm ci`.
2. انسخ `.env.example` إلى `.env.local`، واستبدل القيمة بسر عشوائي طويل (32 حرفًا على الأقل). يمكن توليده محليًا بالأمر `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`.
3. في Vercel افتح **Project Settings → Environment Variables** وأضف `AUTH_SECRET` لبيئة Production وأي بيئة Preview مستخدمة، ثم أعد النشر. لا تستخدم بادئة `NEXT_PUBLIC_` ولا ترفع `.env.local` إلى GitHub.
4. الحسابات الحالية موجودة؛ استخدم الخطوات التالية عند إضافة موظف أو تحديث حسابه فقط.

## إضافة مستخدم أو فرع جديد لاحقًا

**أ. إضافة مستخدم:** استخدم الأمر التالي بقيم تختارها محليًا. لا تضع كلمة المرور في ملف مصدر أو مستند مرفوع إلى GitHub.

```bash
npm run add-admin -- username "password" "الاسم"
```

القيم أعلاه أمثلة توضيحية فقط. استبدلها باسم مستخدم وكلمة مرور تختارها بنفسك (12 حرفًا على الأقل، و72 بايت كحد أقصى). اسم المستخدم يقبل الحروف الإنجليزية والأرقام والنقطة والشرطة والشرطة السفلية، وهو حساس لحالة الأحرف. تكرار اسم المستخدم يحدّث الحساب.

**ب. تغيير كلمة المرور:** شغّل الأمر نفسه باسم المستخدم الحالي وكلمة المرور الجديدة. تبقى بيانات الفرع المحفوظة كما هي ما لم تُدخل قيمة فرع جديدة.

**ج. حذف مستخدم:** احذف كائن المستخدم المطلوب من `config/admins.json` مع الحفاظ على JSON صالح، ثم نفّذ خطوات النشر. لا تحذف حساب بهاء الشخصي دون قصد. لحذف حساب فرع ما زال مسجلًا في بيانات العروض، راجع متطلب حساب واحد لكل فرع واختبار تغطية الفروع قبل النشر.

**د. إضافة حساب فرع:** اقرأ اسم الفرع الفعلي من `data/offerings.ts`، واختر اسم مستخدم إنجليزيًا واضحًا وكلمة مرور فريدة. يدعم الأمر معاملًا رابعًا اختياريًا لحفظ بيانات الفرع:

```bash
npm run add-admin -- branch_username "password" "اسم عرض الفرع" "المدينة — اسم الفرع كما في البيانات"
```

المعامل الرابع للتعريف فقط؛ حسابات الفروع تستطيع الوصول إلى النظام الداخلي كاملًا. لا تُنشئ فرعًا غير مسجل في البيانات. القائمة الحالية تتبع كل زوج مدينة/اسم فرع مميز، بما في ذلك المسميات العامة؛ لا تدمج الأسماء المتشابهة دون التحقق من بيانات المعهد.

**هـ. رفع تغييرات الحسابات:** بعد التحقق والاختبارات، ارفع ملف التجزئات فقط:

البرنامج يحفظ تجزئة bcrypt فقط ولا يطبع كلمة المرور. يمنع إعداد `.npmrc` طباعة npm لمعاملات التشغيل، لكن قد تحتفظ الطرفية بسجل الأمر؛ استخدم جهازًا موثوقًا ولا تشارك سجل الطرفية.

```bash
git add config/admins.json
git commit -m "Update staff accounts"
git push origin main
```

سيعيد Vercel النشر تلقائيًا إذا كان المشروع مرتبطًا بهذا المستودع وفرع الإنتاج هو `main`. تعديل الملف محليًا لا يغيّر حسابات النسخة المنشورة حتى إعادة النشر. لحذف موظف، احذف كائنه من الملف ثم نفّذ خطوات النشر نفسها. أبقِ المستودع خاصًا لأن الملف يحتوي على أسماء الموظفين وتجزئات كلمات المرور.

ملف `STAFF_CREDENTIALS.txt` مخصص لمالك النظام محليًا ويحتوي على كلمات المرور المعروفة عند إنشاء الحسابات. وهو مستثنى صراحةً في `.gitignore`. لا ترفعه ولا تستخدم `git add -f` معه، وحدّث نسختك المحلية عند تغيير كلمة المرور. كذلك لا ترفع `.env.local` أو قيمة `AUTH_SECRET`.

إذا ظهرت رسالة «تعذر تسجيل الدخول حاليًا. يرجى المحاولة لاحقًا»، فهي مشكلة خادم/إعداد، وليست رسالة كلمة مرور غير صحيحة. تحقق من **Vercel → Project Settings → Environment Variables → AUTH_SECRET** ومن إعادة النشر بعد تغييره. يجب أن تكون القيمة عشوائية بطول 32 حرفًا على الأقل وليست قيمة المثال. السجل الآمن `SSTLI_AUTH_LOGIN_FAILED` يساعد على تشخيص الإعداد دون طباعة السر أو كلمات المرور.

## الجلسات والنشر

- الجلسة موقعة بـ HS256 وصالحة لثماني ساعات. اسم ملف الارتباط `sstli_session`، وهو `httpOnly` و`sameSite=lax` و`path=/`، و`secure` في الإنتاج.
- تسجيل الخروج يمسح ملف الارتباط في المتصفح. الجلسات موقعة دون قاعدة بيانات؛ نسخة مسروقة من الرمز تظل صالحة حتى انتهاء مدتها. تغيير `AUTH_SECRET` وإعادة النشر يلغي جميع الرموز السابقة. حذف الحساب يمنع الوصول إلى صفحات البيانات بعد النشر.
- لا توجد كلمات مرور أو تجزئات أو أسرار في بيانات المتصفح. بيانات البرامج تُرسل بعد التحقق من المستخدم على الخادم.
- استخدم إعداد Next.js المعتاد في Vercel: أمر البناء `npm run build` ومجلد الإخراج الافتراضي؛ احذف أي تجاوز يشير إلى `out`. لا تستخدم التصدير الثابت أو GitHub Pages.
- أوقف أي نشر GitHub Pages سابق من إعدادات المستودع؛ إزالة سير العمل تمنع النشر الجديد ولكن لا تحذف النسخة المنشورة سابقًا.
- جميع الصفحات، بما فيها الدخول، تحمل تعليمات منع الفهرسة، مع `robots.txt` و`X-Robots-Tag`. إذا كانت صفحات قد فُهرست سابقًا، استخدم أداة الإزالة في Google Search Console؛ تعليمات المنع لا تزيل النتائج القديمة فورًا.
- فعّل قاعدة تحديد معدل طلبات `/api/auth/login` في Vercel Firewall لحماية الدخول من المحاولات المتكررة عبر جميع نسخ الخادم.

## التحقق المحلي

```bash
npm test
npm run build
npm start
```

ملف الارتباط الآمن في الإنتاج يحتاج HTTPS. استخدم `npm run dev` للاختبار المحلي عبر HTTP، ثم اختبر نسخة Vercel عبر HTTPS بعد إعداد السر والحساب.

## حسابات الفروع المسجلة حاليًا

توجد 15 تسمية فرع مميزة في بيانات العروض، موزعة على ست مدن. لكل تسمية حساب مستقل. الحسابات الشخصية الثلاثة محفوظة دون تغيير.

| اسم المستخدم | الفرع كما في البيانات |
| --- | --- |
| riyadh_hamra | الرياض — حي الحمراء |
| riyadh_laban | الرياض — حي ظهرة لبن |
| riyadh_andalus | الرياض — حي الأندلس |
| dammam_zuhour1 | الدمام — حي الزهور 1 |
| dammam_zuhour2 | الدمام — حي الزهور 2 |
| dammam_shati_west | الدمام — حي الشاطئ الغربي |
| dammam | الدمام — فرع الدمام |
| hafr_waha | حفر الباطن — حي الواحة |
| hafr_masif | حفر الباطن — حي المصيف |
| khamis_dharfa | خميس مشيط — حي الظرفة |
| khamis_square | خميس مشيط — سكوير |
| madinah | المدينة المنورة — فرع المدينة المنورة |
| sakaka | سكاكا / الجوف — فرع سكاكا |
| dammam_shati | الدمام — حي الشاطئ |
| dammam_zuhour | الدمام — حي الزهور |
