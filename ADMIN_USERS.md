# إدارة حسابات موظفي SSTLI

التطبيق خاص بالموظفين. ملف `config/admins.json` يبدأ فارغًا؛ لا يوجد حساب أو كلمة مرور افتراضية.

## الإعداد الأول

1. شغّل `npm ci`.
2. انسخ `.env.example` إلى `.env.local`، واستبدل القيمة بسر عشوائي طويل (32 حرفًا على الأقل). يمكن توليده محليًا بالأمر `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`.
3. في Vercel افتح **Project Settings → Environment Variables** وأضف `AUTH_SECRET` لبيئة Production وأي بيئة Preview مستخدمة، ثم أعد النشر. لا تستخدم بادئة `NEXT_PUBLIC_` ولا ترفع `.env.local` إلى GitHub.
4. أضف أول موظف محليًا كما يلي.

## إضافة مستخدم أو تحديث كلمة مروره

```bash
npm run add-admin -- username "password" "الاسم"
```

القيم أعلاه أمثلة توضيحية فقط. استبدلها باسم مستخدم وكلمة مرور تختارها بنفسك (12 حرفًا على الأقل، و72 بايت كحد أقصى). اسم المستخدم يقبل الحروف الإنجليزية والأرقام والنقطة والشرطة والشرطة السفلية، وهو حساس لحالة الأحرف. تكرار اسم المستخدم يحدّث الحساب.

البرنامج يحفظ تجزئة bcrypt فقط ولا يطبع كلمة المرور. يمنع إعداد `.npmrc` طباعة npm لمعاملات التشغيل، لكن قد تحتفظ الطرفية بسجل الأمر؛ استخدم جهازًا موثوقًا ولا تشارك سجل الطرفية.

```bash
git add config/admins.json
git commit -m "Add staff account"
git push origin main
```

سيعيد Vercel النشر تلقائيًا إذا كان المشروع مرتبطًا بهذا المستودع وفرع الإنتاج هو `main`. تعديل الملف محليًا لا يغيّر حسابات النسخة المنشورة حتى إعادة النشر. لحذف موظف، احذف كائنه من الملف ثم نفّذ خطوات النشر نفسها. أبقِ المستودع خاصًا لأن الملف يحتوي على أسماء الموظفين وتجزئات كلمات المرور.

## الجلسات والنشر

- الجلسة موقعة بـ HS256 وصالحة لثماني ساعات. اسم ملف الارتباط `sstli_session`، وهو `httpOnly` و`sameSite=lax` و`path=/`، و`secure` في الإنتاج.
- تسجيل الخروج يمسح ملف الارتباط في المتصفح. الجلسات موقعة دون قاعدة بيانات؛ نسخة مسروقة من الرمز تظل صالحة حتى انتهاء مدتها. تغيير `AUTH_SECRET` وإعادة النشر يلغي جميع الرموز السابقة. حذف الحساب يمنع الوصول إلى صفحات البيانات بعد النشر.
- لا توجد كلمات مرور أو تجزئات أو أسرار في بيانات المتصفح. بيانات البرامج تُرسل بعد التحقق من المستخدم على الخادم.
- استخدم إعداد Next.js المعتاد في Vercel: أمر البناء `npm run build` ومجلد الإخراج الافتراضي؛ احذف أي تجاوز يشير إلى `out`. لا تستخدم التصدير الثابت أو GitHub Pages.
- أوقف أي نشر GitHub Pages سابق من إعدادات المستودع؛ إزالة سير العمل تمنع النشر الجديد ولكن لا تحذف النسخة المنشورة سابقًا.
- جميع الصفحات، بما فيها الدخول، تحمل تعليمات منع الفهرسة، مع `robots.txt` و`X-Robots-Tag`. إذا كانت صفحات قد فُهرست سابقًا، استخدم أداة الإزالة في Google Search Console؛ تعليمات المنع لا تزيل النتائج القديمة فورًا.
- فعّل قاعدة تحديد معدل طلبات `/api/auth/login` في Vercel Firewall لحماية الدخول من المحاولات المتكررة عبر جميع نسخ الخادم.

## التحقق المحلي

```bash
npm test
npm run build
npm start
```

ملف الارتباط الآمن في الإنتاج يحتاج HTTPS. استخدم `npm run dev` للاختبار المحلي عبر HTTP، ثم اختبر نسخة Vercel عبر HTTPS بعد إعداد السر والحساب.
