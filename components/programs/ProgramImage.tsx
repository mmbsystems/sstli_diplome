"use client";
import {useState} from "react";import Image from "next/image";import {BookOpen} from "lucide-react";
export default function ProgramImage({name,image,imagePosition="center",className="",priority=false}:{name:string;image?:string;imagePosition?:string;className?:string;priority?:boolean}){const [failed,setFailed]=useState(false);return <div className={`program-image ${!image||failed?"placeholder":""} ${className}`}>{image&&!failed?<Image src={image} alt={`صورة ${name}`} width={1254} height={1254} sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" className="program-image-media" style={{objectPosition:imagePosition}} priority={priority} onError={()=>setFailed(true)}/>:<span role="img" aria-label={`لا تتوفر صورة لبرنامج ${name}`}><BookOpen size={48} strokeWidth={1.5}/></span>}</div>}
